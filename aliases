# ─────────────────────────────────────────────
# Shell entry point
# ─────────────────────────────────────────────

SHELL_DIR="$HOME/Code/dotfiles/shell"

source "$SHELL_DIR/core.sh"
source "$SHELL_DIR/general.sh"
source "$SHELL_DIR/viserya.sh"
source "$SHELL_DIR/afterroll.sh"
source "$SHELL_DIR/clickmax.sh"
